In `bill.py`, I identified that the subtotal was missing from `grand_total` by 
running tests in the Testing panel. I then used Run and Debug to set a breakpoint 
inside `grand_total` and stepped through the function line-by-line while examining 
values in the VARIABLES panel. I initially expected the debugger to identify errors, 
but learned that knowing the expected result is necessary to recognize when the code 
is incorrect. The failing test showed me that `grand_total` requires both 
`tip_amount()` and the subtotal, while the debugger helped pinpoint where the 
calculation failed. My actionable goal is to define expected results through tests 
or comments before debugging, then step through the code and examine variables to 
locate calculation errors.